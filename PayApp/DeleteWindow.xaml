<Window x:Class="PayApp.DeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PayApp"
        mc:Ignorable="d"
        Title="" Height="100" Width="280">
    <Grid HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding DeleteText}" Grid.ColumnSpan="2" Grid.Row="0" FontSize="12" Margin="3"/>
        <Button Grid.Column="0" Grid.Row="1" Content="Ja" Click="ConfirmDeleteYes" Margin="3"></Button>
        <Button Grid.Column="1" Grid.Row="1" Content="Nee" Click="ConfirmDeleteNo" Margin="3"></Button>

    </Grid>
</Window>
